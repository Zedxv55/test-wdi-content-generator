(()=>{const targets=()=>document.querySelectorAll('.s21,.card,.page-card,.wp-card,.set-home-card,.box,.result,.out-sec,.item-row');const bind=el=>{if(el._s21)return;el._s21=true;el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--s21-x',((e.clientX-r.left)/r.width*100).toFixed(1)+'%');el.style.setProperty('--s21-y',((e.clientY-r.top)/r.height*100).toFixed(1)+'%')});};const run=()=>targets().forEach(bind);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run);else run();new MutationObserver(run).observe(document.documentElement,{subtree:true,childList:true})})();